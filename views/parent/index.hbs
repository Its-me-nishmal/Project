<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Parent Home</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f2f2f2;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
            background-color: #fff;
            border: 1px solid #e5e5e5;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        h1 {
            text-align: center;
            color: #333;
        }
        .student-info {
            margin-top: 20px;
            padding: 10px;
            background-color: #f5f5f5;
            border: 1px solid #ddd;
            border-radius: 5px;
        }
        .attendance-calendar {
            margin-top: 20px;
            display: grid;
            grid-template-columns: repeat(7, 1fr);
            gap: 5px;
        }
        .day-cell {
            background-color: #f5f5f5;
            border: 1px solid #ddd;
            border-radius: 5px;
            padding: 10px;
            text-align: center;
        }
        .green-dot {
            background-color: #008000;
            width: 10px;
            height: 10px;
            border-radius: 50%;
            margin: 0 auto 5px;
        }
        .red-dot {
            background-color: #FF0000;
            width: 10px;
            height: 10px;
            border-radius: 50%;
            margin: 0 auto 5px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Parent Home</h1>
        <div class="student-info">
            <p>Student Name: {{student.name}}</p>
            <p>Guardian Attendance: <span class="attendance">Online</span></p>
        </div>
        <div class="attendance-calendar">
            {{#each attendances}}
                <div class="day-cell">
                    <p>Date: {{date}}</p>
                    {{#each students}}
                        {{#if this.isPresent}}
                            <div class="green-dot"></div>
                        {{else}}
                            <div class="red-dot"></div>
                        {{/if}}
                    {{/each}}
                </div>
            {{/each}}
        </div>
    </div>
</body>
</html>
